# Eval Cases for weekly-matcher
# Match CVs with scoring

eval_id: weekly-matcher-basic
skill: weekly-matcher
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: score-calc
    name: "Score calculation shown"
    input:
      task: "generate_report"
    expected:
      - contains: "Score"
      - contains: "Calculation"
    timeout: 30
  - id: gap-analysis
    name: "Gap analysis"
    input:
      task: "generate_report"
    expected:
      - contains: "Gap"
      - contains: "Missing"
    timeout: 30
  - id: recommendation
    name: "Clear recommendation"
    input:
      task: "generate_report"
    expected:
      - contains: "Recommend"
      - contains: "Apply"
    timeout: 30
