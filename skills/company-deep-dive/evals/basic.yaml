# Eval Cases for company-deep-dive
# Analyze companies with evidence

eval_id: company-deep-dive-basic
skill: company-deep-dive
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: source-url
    name: "Every claim has URL"
    input:
      task: "generate_report"
    expected:
      - contains: "<a href="
      - contains: "http"
    timeout: 30
  - id: competitor-matrix
    name: "Competitor matrix"
    input:
      task: "generate_report"
    expected:
      - contains: "<table>"
      - contains: "Competitor"
    timeout: 30
  - id: weakness-evidence
    name: "Weaknesses with proof"
    input:
      task: "generate_report"
    expected:
      - contains: "Evidence"
      - contains: "Impact"
    timeout: 30
