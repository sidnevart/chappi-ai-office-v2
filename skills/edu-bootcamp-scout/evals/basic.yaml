# Eval Cases for edu-bootcamp-scout
# Find bootcamps with verification

eval_id: edu-bootcamp-scout-basic
skill: edu-bootcamp-scout
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: official-source
    name: "Official source verified"
    input:
      task: "generate_report"
    expected:
      - contains: "Official"
      - contains: "http"
    timeout: 30
  - id: outcome-data
    name: "Outcome data with sources"
    input:
      task: "generate_report"
    expected:
      - contains: "Outcome"
      - contains: "Placement"
    timeout: 30
