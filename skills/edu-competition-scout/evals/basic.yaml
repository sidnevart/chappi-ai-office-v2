# Eval Cases for edu-competition-scout
# Find competitions with verification

eval_id: edu-competition-scout-basic
skill: edu-competition-scout
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: prize-verify
    name: "Prize verified"
    input:
      task: "generate_report"
    expected:
      - contains: "Prize"
      - contains: "Source"
    timeout: 30
  - id: previous-winners
    name: "Previous winners"
    input:
      task: "generate_report"
    expected:
      - contains: "Winner"
      - contains: "Year"
    timeout: 30
