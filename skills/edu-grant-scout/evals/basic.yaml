# Eval Cases for edu-grant-scout
# Find grants with verification

eval_id: edu-grant-scout-basic
skill: edu-grant-scout
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: amount-verify
    name: "Amount verified"
    input:
      task: "generate_report"
    expected:
      - contains: "Amount"
      - contains: "Source"
    timeout: 30
  - id: eligibility
    name: "Eligibility requirements"
    input:
      task: "generate_report"
    expected:
      - contains: "Eligibility"
      - contains: "Requirements"
    timeout: 30
