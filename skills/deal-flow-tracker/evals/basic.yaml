# Eval Cases for deal-flow-tracker
# Track deals with verification

eval_id: deal-flow-tracker-basic
skill: deal-flow-tracker
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: amount-verify
    name: "Amount verified"
    input:
      task: "generate_report"
    expected:
      - contains: "Amount"
      - contains: "Source"
    timeout: 30
  - id: investor-verify
    name: "Investor verified"
    input:
      task: "generate_report"
    expected:
      - contains: "Investor"
      - contains: "http"
    timeout: 30
  - id: html-output
    name: "Generates HTML"
    input:
      task: "generate_report"
    expected:
      - contains: "<!DOCTYPE html>"
    timeout: 30
