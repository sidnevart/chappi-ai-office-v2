# Eval Cases for competitive-analysis
# Analyze competition with evidence

eval_id: competitive-analysis-basic
skill: competitive-analysis
description: "Evidence-based functionality tests"
version: "2.0"

cases:
  - id: gap-evidence
    name: "Market gap with evidence"
    input:
      task: "generate_report"
    expected:
      - contains: "Evidence"
      - contains: "Source"
    timeout: 30
  - id: investment-calc
    name: "Investment calculation"
    input:
      task: "generate_report"
    expected:
      - contains: "Investment"
      - contains: "$"
    timeout: 30
  - id: feature-matrix
    name: "Feature comparison"
    input:
      task: "generate_report"
    expected:
      - contains: "<table>"
      - contains: "Feature"
    timeout: 30
